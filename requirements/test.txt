atomicwrites>=1.0                        # Required by pytest
attrs>=17.4.0                            # Required by pytest
importlib-metadata>=0.12                 # Required by pytest
more-itertools>=4.0.0                    # Required by pytest
packaging==19.0                          # Required by pytest
pluggy>=0.12,<1.0                        # Required by pytest,tox
py>=1.5.0,<2                             # Required by pytest,tox
six>=1.10.0,<2                           # Required by pytest,tox
wcwidth==0.1.7                           # Required by pytest
pytest==4.6.0
filelock>=3.0.0,<4                       # Required by tox
setuptools>=30.0.0                       # Required by tox
toml>=0.9.4                              # Required by tox
virtualenv>=14.0.0                       # Required by tox
tox==3.12.1
